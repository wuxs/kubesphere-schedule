apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubesphere-scheduling.fullname" . }}-test-connection"
  labels:
    {{- include "kubesphere-scheduling.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubesphere-scheduling.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
